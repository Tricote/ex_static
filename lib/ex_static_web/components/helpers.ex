defmodule ExStaticWeb.Helpers do
  import ExStaticWeb.Gettext

  # def l(to_localize, format \\ :medium)
  # def l(nil, _format), do: ""

  # def l(%Date{} = date, format) do
  #   {:ok, localized} = ExStatic.Cldr.Date.to_string(date, format: format)

  #   localized
  # end
end
